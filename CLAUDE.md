# הנחיות לקלוד קוד

הקובץ הזה נטען אוטומטית כשפותחים את הפרויקט בקלוד קוד. הוא מסביר איך הכלי
בנוי, מה אסור לשבור, ואיפה לגעת כדי לשנות דברים נפוצים.

## מה זה

כלי מקומי להוספת כתוביות בעברית לסרטונים. שרת Flask קטן שמגיש ממשק בדפדפן,
תמלול עם faster-whisper, רינדור כתוביות עם Pillow, וצריבה לווידאו עם ffmpeg.
הכל רץ על המחשב של המשתמש. אין ענן, אין מנוי, אין העלאה לשום מקום.

## הרצה

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install flask Pillow imageio-ffmpeg faster-whisper
python3 app.py          # ואז http://localhost:8769
```

או פשוט לחיצה כפולה על `start.command` במאק, שעושה את כל זה לבד.

## מפת הקבצים

| קובץ | אחריות |
|---|---|
| `app.py` | שרת Flask. נקודות קצה, תמלול, ייצוא, ניהול ערכות ושטח דיסק |
| `ui.html` | כל הממשק והתצוגה החיה. קובץ אחד, בלי בילד, בלי תלויות |
| `styles.py` | פריסטים, מאגר הפונטים, המיפוי לצד ה-CSS |
| `caption_render.py` | מנוע הרינדור. כאן יושב כל הטיפול ב-RTL |
| `transcribe.py` | תמלול, יישור טקסט מודבק, מילות מילוי, הדגשה אוטומטית |

## שלושה חוקים שאסור לשבור

### התצוגה החיה והייצוא חייבים להישאר זהים

`ui.html` מצייר את הכתובית ב-CSS ו-`caption_render.py` מצייר אותה ב-Pillow,
ושניהם קוראים את אותו אובייקט `STYLE`. אם אתה מוסיף מאפיין עיצוב, אתה חייב
לממש אותו בשני המקומות, אחרת מה שהמשתמש רואה זה לא מה שהוא יקבל.

### הפריסה של המילים היא ידנית, לא bidi

המנוע לא מעביר שורה שלמה לאלגוריתם הדו-כיווני. הוא מפרק למילים ומניח אותן
מימין לשמאל בעצמו, ב-`layout_words`. זה מה שמאפשר לצבוע ולהגדיל מילה בודדת,
וזה גם מה שמונע את הבאג שבו "10 פקודות" הופך ל"פקודות 10".

אם תחליף את זה בציור של מחרוזת שלמה, הכל יישבר ברגע שיהיה מספר או מילה
באנגלית באמצע משפט.

### כיוון נקבע לכל מילה בנפרד

`word_dir()` מחזיר `ltr` לטוקן בלי אותיות עבריות. בלי זה `/fork` מצויר
כ-`fork/` והאחוז ב-`30%` קופץ לצד הלא נכון. אל תכפה `rtl` על השורה כולה.

## מוסכמות שכדאי להכיר לפני שנוגעים

**`x_pct` נמדד מהקצה השמאלי של הפריים, גם בממשק וגם ברינדור.** זה נראה
לא אינטואיטיבי בכלי עברי, אבל שני הצדדים מסכימים על זה. אם תשנה אחד מהם
בלי השני, התצוגה תהיה תמונת ראי של הייצוא.

**`y_pct` הוא מרכז הכתובית, לא הקצה העליון.**

**`size_pct` הוא אחוז מגובה הפריים ולא פיקסלים**, כדי שאותו עיצוב ייראה
זהה ב-1080p וב-4K.

**RAQM לא קיים ב-macOS.** ה-wheel של Pillow למאק לא כולל אותו, גם בגרסה 11
וגם ב-12, אז שדרוג לא עוזר. יש מסלול חלופי ב-`_visual_rtl()` שמסדר כל טוקן
עברי ויזואלית. עברית לא דורשת עיצוב הקשרי כמו ערבית, ולכן זה תקף.
`shape()` ו-`_txt_kw()` בוחרים את המסלול הנכון לבד, ואסור להעביר
`direction` ל-Pillow כשאין RAQM.

## שינויים נפוצים

**להוסיף פריסט:** מוסיפים מפתח ל-`PRESETS` ב-`styles.py`. לולאת ה-`setdefault`
בסוף הקובץ תשלים את השדות החסרים. לא צריך לגעת בשום מקום אחר.

**להוסיף פונט:** מפילים קובץ לתיקיית `fonts/`. הוא נסרק אוטומטית.

**לשנות את סגנון הכתובית:** `render_caption()` ב-`caption_render.py` לייצוא,
והפונקציה `tick()` ב-`ui.html` לתצוגה. שניהם.

**לשנות את חלוקת המילים לכתוביות:** `group_into_captions()` ב-`transcribe.py`.

**להוסיף שפה:** בורר השפה יושב ב-`ui.html` ומגיע ל-`transcribe_words()`.
שים לב שהעברת `language=None` מפעילה זיהוי אוטומטי.

**לשנות פורמט ייצוא:** פקודת ה-ffmpeg נמצאת ב-`export_job()` ב-`app.py`.

## מלכודות אמיתיות שנתקלנו בהן

`let` ברמה העליונה של סקריפט לא נתלה על `window`. אם אתה בודק מבחוץ אם
המשתנים נטענו, תבדוק `typeof PRESETS !== 'undefined'` ולא `window.PRESETS`.

אלמנט וידאו בלי מידות מפורשות נופל ל-300x150. `fitVideo()` מחשב את הגודל
האמיתי לפי יחס הממדים, ובלעדיו הכתוביות בתצוגה יוצאות זעירות.

`css_font_stack()` חייב להשתמש ב-`or` ולא ב-`.get(k, default)`, כי הערך
ברירת המחדל מחושב מיד וזורק `KeyError` על מפתח לא קיים.

אם השרת רץ עם קוד ישן, פקדים חדשים פשוט לא מופיעים ואין שגיאה. יש `BUILD`
ב-`app.py` ו-`UI_BUILD` ב-`ui.html`, ואי התאמה מציגה באנר אזהרה.

הרינדור מקבל 300 פריימים לכל היותר בזיכרון בבת אחת. ייצוא ארוך עובד דרך
קאש של מצבים ב-`export_job()`, לא דרך רינדור מחדש של כל פריים.

## איך לבדוק שינוי

אין חבילת טסטים. הדרך המהירה לוודא שלא שברת RTL:

```python
from caption_render import render_caption
img = render_caption((1080,1920), ["חוסך","30%","מהזמן","עם","/fork"], 1,
                     PRESET_DICT, ["Rubik-Black"], emph=[0,1,0,0,1])
img.save("/tmp/t.png")
```

תסתכל על התמונה. המילה הראשונה צריכה להיות הכי ימנית, `30%` צריך להיראות
`30%` ולא `%03`, ו-`/fork` צריך להיראות `/fork` ולא `fork/`.
